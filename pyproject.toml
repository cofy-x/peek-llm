[project]
name = "peek-llm-tools"
version = "0.1.0"
description = "Local validation and data preparation for Peek LLM"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
required-version = ">=0.11.25"

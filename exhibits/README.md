# Exhibits

Each published exhibit belongs in this directory as one lowercase kebab-case `.html` file. Copy `templates/exhibit.html`, then maintain that file directly as both source and published artifact. Do not create a parallel source or distribution copy. Keep every runtime resource inline; do not generate or bundle the exhibit from other files. Follow the review contract in `CONTRIBUTING.md`.

The project skeleton intentionally contains no knowledge exhibit yet.

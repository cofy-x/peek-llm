# Exhibits

Each published exhibit belongs in this directory as one lowercase kebab-case `.html` file. Copy `templates/exhibit.html`, keep every runtime resource inline, and follow the review contract in `CONTRIBUTING.md`.

The project skeleton intentionally contains no knowledge exhibit yet.
